<% if signed_in? %>
  <p>You are signed in, but nothing else is implemented yet. Whoops!</p>
  <!-- Render forms for creating stuff, etc. -->
<% else %>
  <p>There isn't really anything here yet.</p>

  <%= link_to "Sign up now!", signup_path, :class => "signup_button round" %>
<% end %>
