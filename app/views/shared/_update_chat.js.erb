<% if @messages.any? %>
  $('#messages').append('<%= raw escape_javascript(render(@messages)) %>');
  $('#messages').animate({
    scrollTop: $('#messages').attr('scrollHeight') - $('#messages').height() }, 500);
  last_message_id = <%= @messages.last.id %>;
<% end %>
