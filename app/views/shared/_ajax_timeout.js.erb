<%
if !local_assigns.has_key?(:path) or !path
  path = request.env['PATH_INFO']
  path += '.js' if !request.xhr? 
end
%>
setTimeout(function(){
    $.getScript('<%= path %>?last_message=' + last_message_id + '&last_turn=' + last_turn)
  }, <%= UPDATE_DELAY %>)
